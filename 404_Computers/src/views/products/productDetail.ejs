<!DOCTYPE html>
<html lang="es">
<%- include('../partials/head.ejs') %>
<head>
    <script src="/javascripts/productPhotos.js" defer></script>
</head>
<body>
  <div class="container_Page">
    <%- include('../partials/header.ejs') %>   
    <main>
        <div class="titulo">
            <h1><%= productToDisplay.name %></h1>
            <hr>
        </div>

        <!-- The Modal -->
        <div id="myModal" class="modal">

        <!-- The Close Button -->
        <span class="close">&times;</span>

        <!-- Modal Content (The Image) -->
        <img class="modal-content" id="img01">

        </div> 

        <div class="productPhotoContainer">

            <section class="productPhoto">
                <!-- Aca va la imagen Principal  -->
                <img src="/images/products/<%= productToDisplay.image[0] %>" alt="" id="expandedImg" class="imageModal" style="cursor: zoom-in; width: 100%; height: 420px; object-fit: scale-down;">
                <!-- Aca va la Grilla de Fotos del producto  -->
                <div class="photosGalleryBox">
                    <% productToDisplay.image.forEach(element => { %>
                        <img class="miniPhoto" src="/images/products/<%= element %>" onclick="productGridImages(this)">
                    <% }) %>
                </div>
            </section>
    
            <section class="subtitulos">

                <div class="subtitulosBG">
                    <h1 class="precio"> $<%= productToDisplay.price %></h1>
                    <p class="precioEspecial"> PRECIO ESPECIAL</p>
                    <p class="descuento"> <%= productToDisplay.discount %> % OFF</p>
                    <p class="precioLista"> Precio Lista $<%= productToDisplay.priceList %></p>

                    <section class="iconDetailProduct">
                        <label><i class="fas fa-money-check"></i>12 Cuotas sin interés</label>
                        <label><i class="fas fa-store-alt"></i>Retirar en local gratis</label>
                        <label><i class="fas fa-shipping-fast"></i>Envios a todo el país</label>
                        <label><i class="far fa-check-circle"></i>En stock</label>
                        <label><i class="fas fa-award"></i>Garantía 12 Meses</label>
                    </section>

                    <div class="buy_Section">
                        <input class="input-style" type="button" value="COMPRAR" title="COMPRAR" class="comprar">
                    </div>

                    <div class="redes">
                        <i class="fab fa-facebook"></i>
                        <i class="fab fa-twitter"></i>
                        <i class="fab fa-instagram"></i>
                    </div>

                    <div class="favorite_Section">
                        <button class="input-style" type="button"> Agregar a Favoritos <i class="far fa-heart"></i></button>
                    </div>
                </div>

            </section>

        </div>
    
    
        <div class="especificaciones">
            <hr>
            <h3 class="h3"> Especificaciones</h3>
            <span class="negrita">
                <%= productToDisplay.description %>
            </span>
        </div>

        <div class="especificaciones">
            <hr>
            <h3> Preguntas Frecuentes</h3>
            <span class="negrita">¿Viene con windows? </span>
            <span class=" respuesta">viene con windows 10 instalado</span>
            <span class="negrita">¿Cuánto tarda en llegar ?</span>
            <span class=" respuesta">En la sección envios, seleccionando la zona podrás ver el tiempo estimado</span>
            <span class="negrita">¿Viene con perifericos?</span>
            <span class=" respuesta">Este modelo no los trae</span>
        </div>
        <%- include('../partials/slider.ejs') %>  
    </main>
    <%- include('../partials/footer.ejs') %> 
  </div>
</body>
</html>